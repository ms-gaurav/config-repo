server:
  port: 8070
spring:
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
eureka:
  #instance:
  #  hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
   # serviceUrl:
   #   defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    
